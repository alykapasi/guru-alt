import { NotebookText } from "lucide-react";
import { PlaceholderPage } from "../components/PlaceholderPage";

export function Lessons() {
  return (
    <PlaceholderPage
      icon={NotebookText}
      title="Lessons"
      description="Your lesson plan and guided practice sessions — with the practice-item side panel — land in a follow-up slice."
    />
  );
}
