import { Upload } from "lucide-react";
import { PlaceholderPage } from "../components/PlaceholderPage";

export function Uploads() {
  return (
    <PlaceholderPage
      icon={Upload}
      title="Uploads"
      description="Upload documents, links, and recordings for the tutor to draw on, and browse your ingested materials — lands in a follow-up slice."
    />
  );
}
