import { LayoutDashboard } from "lucide-react";
import { PlaceholderPage } from "../components/PlaceholderPage";

export function Dashboard() {
  return (
    <PlaceholderPage
      icon={LayoutDashboard}
      title="Dashboard"
      description="Mastery, retention, streaks, and your learner profile — spacious cards, moderate gamification — land in a follow-up slice."
    />
  );
}
