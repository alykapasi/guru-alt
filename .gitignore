# Python-generated files
__pycache__/

# Mutation-testing scratch: the harness writes <file>.bak beside the file it mutates and
# moves it back afterwards. A run interrupted between the two leaves one behind, and one was
# committed by a `git add -A` before this line existed.
*.bak
*.py[cod]
*$py.class
*.so
build/
dist/
wheels/
*.egg-info
MANIFEST

# Virtual environments
.venv

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing & tool caches
.pytest_cache/
.ruff_cache/
.ty_cache/
.coverage
htmlcov/

# Database
*.db
*.sqlite
*.sqlite3

# Environment variables
.env
.env.local
.env.*.local

# MLflow local tracking store (experiment sweeps)
mlruns/

# Sweep artifact staging dir (kept outside mlruns/ so it isn't scanned as an experiment)
sweep-artifacts/

# Mined real-data eval datasets (Phase 9b) — never committed
tests/eval/datasets/*.json
.playwright-mcp/
